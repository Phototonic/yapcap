# Workflows

- `ci.yml` defines continuous integration checks for pull requests and pushes.
- `release.yml` builds and publishes release artifacts and package metadata.

Keep workflow toolchains and package names aligned with `Cargo.toml` and the packaging manifests.
