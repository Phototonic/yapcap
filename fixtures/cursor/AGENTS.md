# Cursor Fixtures

- `probe.py` experiments with Cursor API requests.
- `auth_me_response.json` captures Cursor identity data.
- `oauth_token_response.json` captures a Cursor token response.
- `usage_summary_response.json` captures the usage summary consumed by the Cursor provider.
