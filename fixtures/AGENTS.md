# Fixtures

Provider subdirectories contain captured API responses and small Python probes used to understand or reproduce provider behavior. Fixtures are test/development data, not runtime configuration. Keep secrets out of new captures and preserve the response shape needed by the corresponding Rust parser.
