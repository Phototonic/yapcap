# Codex Fixtures

- `probe.py` experiments with Codex OAuth and usage requests.
- `oauth_token_response.json` captures an OAuth token response.
- `oauth_usage_response.json` captures the usage response consumed by the Codex provider.
