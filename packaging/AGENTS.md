# Packaging

- `io.github.TopiCsarno.YapCap.json` is the Flatpak manifest.
- `cargo-sources.json` records vendored Cargo dependencies for reproducible Flatpak builds.

Keep package identifiers, executable paths, and bundled resources aligned with `Cargo.toml` and `resources/`.
