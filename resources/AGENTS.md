# Resources

- `app.desktop` and `app-debug.desktop` define desktop launchers.
- `app.metainfo.xml` contains AppStream metadata.
- `icon.svg` is the application icon.
- `providers/` contains provider artwork.
- `screenshots/` contains README and release screenshots.
- `ui-prototype/` contains throwaway UI exploration artifacts.
