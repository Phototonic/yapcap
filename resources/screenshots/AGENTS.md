# Screenshots

These PNGs document the applet and its provider, settings, account, theme, and zoomed views. Images are referenced by the root `README.md` and AppStream metadata; older captures may remain for release history. Capture updated source images when the UI changes, then adjust input names and crop geometry in `scripts/make_zoom_screenshots.py` as needed to regenerate the published set.
