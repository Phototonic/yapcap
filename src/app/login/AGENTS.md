# Login

- `mod.rs` provides shared login state, messages, and flow dispatch.
- `flows/` contains current provider login implementations.
- `legacy/` contains older login helpers retained for provider compatibility.
- `login_flow_tests/` contains login-flow integration-style unit tests.
