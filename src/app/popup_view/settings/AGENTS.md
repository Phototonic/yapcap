# Settings Views

- `about.rs` renders version, update, and project information.
- `accounts.rs` renders provider account settings.
- `general.rs` renders display and refresh settings. Provider toggles live in the parent `settings.rs` and the account settings page.
- `accounts/` contains reusable account rows and login controls.
