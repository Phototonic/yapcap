# Settings Views

- `about.rs` renders version, update, and project information.
- `accounts.rs` renders provider account settings.
- `general.rs` renders display, refresh, and provider-toggle settings.
- `settings/accounts/` contains reusable account rows and login controls.
