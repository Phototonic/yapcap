# OpenCode Go Provider

- `mod.rs` exposes OpenCode Go provider operations.
- `account.rs` manages labeled API-key accounts.
- `login.rs` implements API-key login and reauthentication.
- `opencode.rs` supports optional OpenCode key prefill and active-account matching.
- `storage.rs` persists OpenCode Go private keys and account metadata.
