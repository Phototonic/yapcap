# Claude Account Helpers

- `tests.rs` tests Claude account identity, metadata, and deduplication behavior.

The parent `account.rs` owns the account implementation; this directory is reserved for account-focused test/support modules.
