# Provider Registry

- `tests.rs` tests provider registration, capabilities, and shared account actions.
- `account_facts_tests.rs` tests provider account-settings facts and contracts.

The parent `registry.rs` is the runtime/UI entry point for provider operations.
