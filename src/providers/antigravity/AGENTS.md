# Antigravity Provider

- `mod.rs` exposes the provider implementation.
- `account.rs` defines managed account identity and storage behavior.
- `login.rs` implements Google OAuth login.
- `plan_label.rs` formats plan labels.
- `quota.rs` parses and groups model quota windows.
