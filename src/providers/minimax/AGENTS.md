# Minimax Provider

- `mod.rs` exposes Minimax provider operations.
- `account.rs` manages labeled API-key accounts.
- `login.rs` implements API-key login and reauthentication.
- `opencode.rs` supports optional one-time OpenCode key prefill and active matching.
- `storage.rs` persists Minimax private keys and account metadata.
