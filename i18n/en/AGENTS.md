# English Translation

- `yapcap.ftl` contains the English Fluent messages used by the UI.

Keep message identifiers stable when changing copy; update Rust call sites and other locales together when adding identifiers.
