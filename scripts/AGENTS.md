# Development Scripts

- `make_zoom_screenshots.py` crops existing full-screen captures into hero, detail, provider, settings, and theme images. Its default inputs are dated captures under `~/Pictures/Screenshots`; `--source-dir` and `--destination-dir` override the directories.

These scripts are development utilities and are not part of the released binary.
